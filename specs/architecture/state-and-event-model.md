# 状态与事件模型

- 状态：approved
- 当前性：当前有效

## 关键前端状态

- `navigation_state`：当前一级页面
- `windowMode`：当前工作态
- `gallery_view_mode`：图库管理当前视图
- `recent_searches`：搜索历史
- 冷启动导入状态：未导入 / 选择中 / 导入中 / 导入完成

## 关键事件

- 搜索框聚焦与发起搜索
- 首页快速导入动作
- 图库管理视图切换
- 窗口布局应用与偏好保存
- 高级入口曝光与点击

## 当前设计要求

- 导入入口可以分流，但导入执行状态必须统一
- 窗口模式变化要有唯一来源，避免“启动偏好”和“当前工作态”互相打架
- 模块文档描述长期状态边界，feature 文档描述具体事件链路
