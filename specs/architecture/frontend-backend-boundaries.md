# 前后端边界

- 状态：approved
- 当前性：当前有效

## 前端负责

- 页面职责分层与交互文案
- 搜索主路径、图库管理和设置的路由与状态组织
- 导入入口分流与进度展示
- 窗口模式偏好写入与布局请求触发

## 后端负责

- 图片导入协议的统一接收、展开、过滤和总数统计
- OCR、向量、自动提示词等索引构建
- 窗口布局应用与窗口偏好持久化
- 私有角色库运行时读取与相关检索能力

## 重要约束

- 前端不维护两套导入状态机
- 设置修改应尽量即时生效，不依赖手动重建索引
- 私有角色库维护能力与普通用户路径分层
